<div class={{@class}}>
  <div class='label'>
    {{@label}}
  </div>
  <div>
    {{#each @options as |option|}}
      <Checkbox
        @value={{this.getValue option.label}}
        @onChange={{fn this.onChange option.label}}
        @option={{option}}
      />
    {{/each}}
  </div>
</div>